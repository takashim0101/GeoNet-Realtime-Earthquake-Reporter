# 🔐 API keys and configuration for GeoNet × Stats NZ × Ollama integration
# Rename this file to `.env` and replace placeholder values with your actual keys

# ✅ Stats NZ Spatial Query API key
# Get your key from https://datafinder.stats.govt.nz/
STATS_NZ_API_KEY=your_stats_nz_api_key_here

# ✅ Ollama local server URL
# Default is localhost. Change only if your Ollama server runs on a different IP.
OLLAMA_API_URL=http://localhost:11434

# ✅ Optional: Gemini or other cloud LLM API key (if used)
# Leave blank if using only local LLMs via Ollama
GEMINI_API_KEY=your_google_gemini_api_key_here

# ✅ Optional: Custom model name for Ollama
# Example: llama3, mistral, or any model you've pulled
OLLAMA_MODEL_NAME=llama3

# ✅ Optional: Discord Webhook URL for real-time notifications
# Create a webhook in your Discord server settings
DISCORD_WEBHOOK_URL=your_discord_webhook_url_here
